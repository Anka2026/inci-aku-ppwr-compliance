# İnci Akü PPWR Compliance Suite — Render Blueprint
# Dashboard: New → Blueprint → bu repo
services:
  - type: web
    name: inci-ppwr-compliance
    runtime: docker
    plan: starter
    region: frankfurt
    healthCheckPath: /api/health
    envVars:
      - key: INCI_PPWR_WEB
        value: "1"
      - key: INCI_PPWR_VERSION
        value: "1.0.0"
      - key: INCI_PPWR_WORKSPACE_ROOT
        value: /data/workspace
      - key: INCI_PPWR_DELIVERY_ROOT
        value: /data/delivery
      - key: INCI_PPWR_ADMIN_USER
        value: admin
      - key: INCI_PPWR_ADMIN_PASSWORD
        generateValue: true
      - key: INCI_PPWR_JWT_SECRET
        generateValue: true
      - key: INCI_PPWR_AUTH
        value: "1"
    disk:
      name: ppwr-data
      mountPath: /data
      sizeGB: 25
